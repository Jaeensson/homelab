apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
spec:
  chartRef:
    kind: OCIRepository
    name: traefik
  interval: 1h
  values:
    providers:
      kubernetesCRD:
        enabled: true
      kubernetesGateway:
        enabled: true